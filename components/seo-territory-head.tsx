/**
 * DEPRECATED: Questo componente non è utilizzato.
 * Gli schema SEO territorio sono già implementati in app/layout.tsx
 * Mantenuto per riferimento futuro.
 */
export function TerritorySEOHead() {
  // Ritorna null - non utilizzato (schema già in layout.tsx)
  return null
}

